// @ts-check

/**
 * @type {import('next').NextConfig}
 **/
const config = {
    typescript: {
        ignoreBuildErrors: true,
    },
    // Remove the redirects section
};

module.exports = config;
